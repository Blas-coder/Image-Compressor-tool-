<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>Compress Images Online Free - No Quality Loss (Private & Offline)</title>
    <meta name="description" content="Reduce image file size instantly in your browser. Supports JPG, PNG, WEBP. No uploads to server: 100% private and secure image compression.">
    <meta name="keywords" content="compress image offline, reduce photo size javascript, secure image optimizer, client-side image compression, free photo shrinker">
    
    <meta property="og:title" content="Secure Image Compressor - 100% Offline">
    <meta property="og:description" content="Compress your photos without sending them to any server. Faster, Safer, Free.">
    <meta property="og:type" content="website">

    <style>
        :root {
            --primary: #8b5cf6; /* Purple for "Creative" vibe */
            --primary-dark: #7c3aed;
            --success: #10b981;
            --bg: #f3f4f6;
            --text: #1f2937;
            --card-bg: #ffffff;
            /* Webmaster / Money Colors */
            --premium-bg: #f0fdf4; 
            --premium-border: #86efac;
            --premium-text: #14532d;
            --delete: #ef4444;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg);
            color: var(--text);
            line-height: 1.6;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            max-width: 800px;
            width: 100%;
            background: var(--card-bg);
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.1);
        }

        header { text-align: center; margin-bottom: 30px; }
        h1 { font-size: 2.2rem; color: #111827; margin-bottom: 10px; letter-spacing: -0.02em; }
        .subtitle { color: #6b7280; font-size: 1.1rem; }
        .badge {
            background: #ede9fe; color: #5b21b6; 
            padding: 4px 12px; border-radius: 20px; 
            font-size: 0.85rem; font-weight: 700;
            display: inline-block; margin-top: 10px;
        }

        /* DRAG & DROP ZONE */
        .drop-zone {
            border: 2px dashed #d1d5db;
            border-radius: 16px;
            padding: 50px 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            background: #f9fafb;
            position: relative;
        }
        
        .drop-zone:hover, .drop-zone.dragover {
            border-color: var(--primary);
            background: #f5f3ff;
            transform: scale(1.01);
        }

        .drop-zone p { font-size: 1.2rem; color: #4b5563; margin-top: 15px; font-weight: 500; }
        .drop-icon { font-size: 48px; color: #9ca3af; }
        #file-input { position: absolute; width: 100%; height: 100%; top: 0; left: 0; opacity: 0; cursor: pointer; }

        /* SETTINGS AREA */
        .settings-area {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
            flex-wrap: wrap;
        }
        .setting-group { display: flex; align-items: center; gap: 10px; font-size: 0.9rem; color: #4b5563; }
        input[type="range"] { accent-color: var(--primary); cursor: pointer; }

        /* FILE LIST */
        #file-list-container { margin-top: 30px; }
        .file-item {
            background: #f8fafc;
            padding: 15px;
            margin-bottom: 12px;
            border-radius: 12px;
            border: 1px solid #e5e7eb;
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-between;
            gap: 15px;
            transition: transform 0.2s;
        }
        .file-info { display: flex; align-items: center; gap: 12px; flex: 1; min-width: 200px; }
        .file-name { font-weight: 600; color: #374151; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 200px; }
        .file-stats { font-size: 0.85rem; color: #6b7280; display: flex; gap: 10px; align-items: center; }
        .savings { color: var(--success); font-weight: bold; font-size: 0.9rem; }
        
        .download-btn {
            background: var(--primary); color: white; border: none;
            padding: 8px 16px; border-radius: 8px; font-weight: 600;
            cursor: pointer; font-size: 0.9rem; text-decoration: none;
            display: inline-flex; align-items: center; gap: 5px;
            transition: background 0.2s;
        }
        .download-btn:hover { background: var(--primary-dark); }
        .download-btn.disabled { background: #d1d5db; cursor: wait; }

        /* PREMIUM CARD (WEBMASTER/SEO HOOK) */
        .premium-card {
            margin-top: 50px;
            background: linear-gradient(135deg, var(--premium-bg) 0%, #ffffff 100%);
            border: 1px solid var(--premium-border);
            padding: 25px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
        }
        .premium-content { flex: 1; }
        .premium-title { color: #14532d; font-weight: 800; font-size: 1.2rem; margin-bottom: 5px; }
        .premium-desc { font-size: 0.95rem; color: #15803d; }
        
        .affiliate-btn {
            background: #16a34a; color: white; text-decoration: none;
            padding: 12px 24px; border-radius: 10px; font-weight: 700;
            transition: all 0.2s; box-shadow: 0 4px 6px rgba(22, 163, 74, 0.2);
        }
        .affiliate-btn:hover { transform: translateY(-2px); box-shadow: 0 6px 12px rgba(22, 163, 74, 0.3); background: #15803d; }

        footer { margin-top: 50px; font-size: 0.85rem; color: #9ca3af; text-align: center; }
    </style>
    
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/browser-image-compression@2.0.2/dist/browser-image-compression.js"></script>
</head>
<body>

    <div class="container">
        <header>
            <h1>Smart Image Compressor</h1>
            <p class="subtitle">Reduce file size up to 90% without losing quality.</p>
            <span class="badge">⚡ Powered by your Browser (0% Server Upload)</span>
        </header>

        <div class="drop-zone" id="drop-zone">
            <div class="drop-icon">🖼️</div>
            <p>Drop your JPG, PNG or WEBP here</p>
            <span style="color:#9ca3af; font-size: 0.9rem">(or click to browse)</span>
            <input type="file" id="file-input" accept="image/jpeg, image/png, image/webp" multiple>
        </div>

        <div class="settings-area">
            <div class="setting-group">
                <label>Quality:</label>
                <input type="range" id="quality" min="0.1" max="1" step="0.1" value="0.8">
                <span id="quality-val" style="font-weight:bold; color:var(--primary)">80%</span>
            </div>
            <div class="setting-group">
                <label>Max Width:</label>
                <select id="max-width" style="padding:5px; border-radius:6px; border:1px solid #d1d5db">
                    <option value="1920">1920px (Full HD)</option>
                    <option value="1280">1280px (HD)</option>
                    <option value="800">800px (Blog)</option>
                    <option value="0" selected>Original Size</option>
                </select>
            </div>
        </div>

        <div id="file-list-container"></div>

        <div class="premium-card">
            <div class="premium-content">
                <div class="premium-title">🚀 Is your website slow?</div>
                <div class="premium-desc">Stop compressing manually. Install this plugin to optimize all your WordPress images automatically and boost your SEO.</div>
            </div>
            
            <a href="HREF_DE_TU_AFILIADO_AQUI" 
               target="_blank" 
               rel="nofollow sponsored" 
               class="affiliate-btn">
                Optimize My Site Now &rarr;
            </a>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 Zero-Ops Tools. Privacy First Architecture.</p>
    </footer>

    <script>
        const fileInput = document.getElementById('file-input');
        const dropZone = document.getElementById('drop-zone');
        const listContainer = document.getElementById('file-list-container');
        const qualitySlider = document.getElementById('quality');
        const qualityVal = document.getElementById('quality-val');
        const maxWidthSelect = document.getElementById('max-width');

        // Update Slider UI
        qualitySlider.oninput = (e) => qualityVal.innerText = Math.round(e.target.value * 100) + "%";

        // Drag & Drop Logic
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
            dropZone.addEventListener(eventName, preventDefaults, false);
        });
        function preventDefaults(e) { e.preventDefault(); e.stopPropagation(); }
        ['dragenter', 'dragover'].forEach(name => dropZone.classList.add('dragover'));
        ['dragleave', 'drop'].forEach(name => dropZone.classList.remove('dragover'));

        dropZone.addEventListener('drop', (e) => handleFiles(e.dataTransfer.files));
        fileInput.addEventListener('change', (e) => handleFiles(e.target.files));

        async function handleFiles(files) {
            Array.from(files).forEach(processFile);
        }

        async function processFile(file) {
            // 1. Create UI Element (Loading State)
            const id = Math.random().toString(36).substring(7);
            const item = document.createElement('div');
            item.className = 'file-item';
            item.id = id;
            item.innerHTML = `
                <div class="file-info">
                    <span style="font-size:1.5rem">📄</span>
                    <div>
                        <div class="file-name">${file.name}</div>
                        <div class="file-stats">Original: ${(file.size/1024).toFixed(1)} KB</div>
                    </div>
                </div>
                <button class="download-btn disabled" disabled>⏳ Compressing...</button>
            `;
            listContainer.prepend(item);

            try {
                // 2. Configure Compression Options
                const options = {
                    maxSizeMB: 1, // Start logic
                    maxWidthOrHeight: maxWidthSelect.value == "0" ? undefined : parseInt(maxWidthSelect.value),
                    useWebWorker: true,
                    initialQuality: parseFloat(qualitySlider.value)
                };

                // 3. Compress using Library
                const compressedFile = await imageCompression(file, options);

                // 4. Calculate Savings
                const savings = ((file.size - compressedFile.size) / file.size * 100).toFixed(0);
                
                // 5. Update UI with Success
                const downloadUrl = URL.createObjectURL(compressedFile);
                item.innerHTML = `
                    <div class="file-info">
                        <span style="font-size:1.5rem">✅</span>
                        <div>
                            <div class="file-name">${file.name}</div>
                            <div class="file-stats">
                                ${(compressedFile.size/1024).toFixed(1)} KB 
                                <span class="savings">(-${savings}%)</span>
                            </div>
                        </div>
                    </div>
                    <a href="${downloadUrl}" download="min-${file.name}" class="download-btn">
                        ⬇️ Download
                    </a>
                `;

            } catch (error) {
                console.error(error);
                item.innerHTML += `<span style="color:red; margin-left:10px">Error</span>`;
            }
        }
    </script>
</body>
</html>
